kind: Service
metadata:
  name: kubedemoapp-service1
spec:
  selector:
    app: kubedemoapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30081
  type: NodePort
